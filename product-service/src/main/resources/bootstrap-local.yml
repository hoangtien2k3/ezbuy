app:
  config:
    keycloak:
      url: http://10.207.252.78:8430/auth
      realm: sme-portal
logging:
  config: classpath:log4j2-local.xml
spring:
  application:
    name: sme-product-service
  cloud:
    kubernetes:
      enabled: false
    config:
      enaled: false
  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: ${app.config.keycloak.url}/realms/sme-portal/protocol/openid-connect/certs
      keycloak:
        client-id: web-client
      client:
        provider:
          oidc:
            token-uri: ${app.config.keycloak.url}/realms/${app.config.keycloak.realm}/protocol/openid-connect/token
        registration:
          oidc:
            client-id: sme-auth-service
            client-secret: YxzXsTW2sjHh3jBBYOmrIOzYT6DKE6vV
            authorization-grant-type: client_credentials
management:
  endpoint:
    health:
      show-details: never
  health:
    redis:
      enabled: false
    elasticsearch:
      enabled: false
    mail:
      enabled: false
